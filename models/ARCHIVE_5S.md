# Archive — ancien Fanal 5 secondes

Le live n’est plus un jouet 5 s. Les dumps LightGBM 1s (`models/fanal_sec_*.json`,
`netlify/functions/_models/`) et le paper Coinbase 5 s (`lib/paper.ts`, ledger v1)
restent dans le repo comme artefacts. Ils ne sont **pas** le scoreur `/api/predict`
ni le paper Polymarket (ledger **v3**, store `fanal-paper-poly`).

Ne pas mélanger les carnets.
